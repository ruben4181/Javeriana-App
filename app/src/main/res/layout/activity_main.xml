<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".LoginActivity"
        android:background="@color/colorPrimary">

    <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp" app:srcCompat="@drawable/logo"
            android:id="@+id/logo_login"
            android:layout_centerHorizontal="true" android:layout_alignParentTop="true"
            android:layout_marginTop="150dp"/>
    <EditText
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:inputType="textPersonName"
            android:textColorHint="@color/colorHintText"
            android:textColor="@android:color/white"
            android:hint="Nombre de usuario"
            android:ems="10"
            android:textStyle="bold"
            android:layout_centerHorizontal="true" android:layout_below="@+id/logo_login" android:id="@+id/username_login"
            android:layout_marginTop="22dp"/>
    <EditText
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:textColorHint="@color/colorHintText"
            android:hint="Contraseña"
            android:textColor="@android:color/white"
            android:inputType="textPassword"
            android:textStyle="bold"
            android:ems="10"
            android:id="@+id/password_login"
            android:layout_marginTop="8dp" android:layout_below="@+id/username_login"
            android:layout_alignStart="@+id/username_login" android:layout_marginStart="0dp"/>
    <Button
            android:text="Iniciar Sesion"
            android:textColor="#fff"
            android:background="@drawable/def_button"
            android:layout_width="300dp"
            android:textStyle="bold"
            android:layout_height="wrap_content" android:layout_centerHorizontal="true"
            android:id="@+id/sign_in_login"
            android:layout_marginTop="12dp" android:layout_below="@+id/password_login"
            android:onClick="login"/>
    <TextView
            android:text="¿Olvidaste la contraseña?"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:layout_centerHorizontal="true"
            android:id="@+id/forgot_password_login"
            android:textColor="#fff"
            android:layout_marginTop="14dp" android:layout_below="@+id/sign_in_login"/>
</RelativeLayout>